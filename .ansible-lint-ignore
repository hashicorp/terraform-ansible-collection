# changelogs/changelog.yaml is generated by antsibull-changelog, which emits
# block sequences at the same indentation as their parent key. This conflicts
# with yamllint's indent-sequences rule and is reverted on every release, so
# the indentation rule is ignored for the generated changelog database only.
changelogs/changelog.yaml yaml[indentation]
